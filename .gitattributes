# Repozitář vznikl na macOS (LF), studenti budou commitovat z Windows
# (CRLF) — bez normalizace by to způsobovalo zbytečné diffy a varování
# ve Visual Studiu.
* text=auto

# binární soubory — pro jistotu do budoucna
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
